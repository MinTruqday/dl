apiVersion: apps/v1
kind: Deployment
metadata:
  name: usage-deployment
  namespace: doclib-production
  labels:
    app: usage
spec:
  replicas: 3
  selector:
    matchLabels:
      app: usage
  template:
    metadata:
      labels:
        app: usage
    spec:
      containers:
        - name: usage
          image: doclib-registry/usage:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8014
          envFrom:
            - configMapRef:
                name: doclib-config
            - secretRef:
                name: doclib-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8014
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8014
            initialDelaySeconds: 15
            periodSeconds: 5
            successThreshold: 1
---
apiVersion: v1
kind: Service
metadata:
  name: usage
  namespace: doclib-production
spec:
  selector:
    app: usage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8014

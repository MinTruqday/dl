apiVersion: apps/v1
kind: Deployment
metadata:
  name: conversations-deployment
  namespace: doclib-production
  labels:
    app: conversations
spec:
  replicas: 3
  selector:
    matchLabels:
      app: conversations
  template:
    metadata:
      labels:
        app: conversations
    spec:
      containers:
        - name: conversations
          image: doclib-registry/conversations:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: doclib-config
            - secretRef:
                name: doclib-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
            successThreshold: 1
---
apiVersion: v1
kind: Service
metadata:
  name: conversations
  namespace: doclib-production
spec:
  selector:
    app: conversations
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

apiVersion: apps/v1
kind: Deployment
metadata:
  name: compilation-deployment
  namespace: doclib-production
  labels:
    app: compilation
spec:
  replicas: 3
  selector:
    matchLabels:
      app: compilation
  template:
    metadata:
      labels:
        app: compilation
    spec:
      containers:
        - name: compilation
          image: doclib-registry/compilation:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: doclib-config
            - secretRef:
                name: doclib-compilation-secrets
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
            successThreshold: 1
---
apiVersion: v1
kind: Service
metadata:
  name: compilation
  namespace: doclib-production
spec:
  selector:
    app: compilation
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

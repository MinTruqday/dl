import ProvisionLayout from "@/features/content/components/ProvisionLayout";

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <ProvisionLayout>{children}</ProvisionLayout>;
}

<USER_REQUEST>
Tôi đã khởi động dockẻ, tôi cũng có tải 3 quyển sách để test cái luồng embedding, chunking,... như nào rồi rồi đây là yêu cầu để test và prompt để test của tôi, hãy thực hiện chúng xong tự đánh giá coi là từng chức năng của agentic ai hiẹn tại đạt bao nhiêu trên 10, đứng ở mức độ nào, : critical, lơ, medium hy high và sau đó đưa ra hướng giải quyết giúp tôi, tiến trình này sẽ lâu nhưng phải hoạt động đến lúc đưa hết kết quả cho tôi
Chào bạn, dưới góc độ của một Prompt Engineer, tôi đã phân tích kiến trúc phân tuyến (Router) và các Node/Tool được định nghĩa trong hệ thống LangGraph của dự án DocLib.

Hệ thống Agentic AI của bạn được chia thành nhiều nhánh xử lý khác nhau (Fast Chat, Action, Knowledge). Dưới đây là bộ Prompt Test (Test Suite) bao phủ toàn bộ 100% các công cụ (Tools) đã được lập trình để bạn kiểm thử luồng `/luong-du-lieu`.

### 1. Nhóm Kiểm Thử: Giao tiếp cơ bản (Bypass LangGraph)

Nhóm này kiểm tra xem Router Agent có nhận diện đúng Intent là "chat" để đẩy thẳng qua `HFInferenceChat` (LLaMA) nhằm tăng tốc độ phản hồi hay không, bỏ qua đồ thị suy luận.

* **Prompt 1:** "Xin chào, bạn là ai và bạn đóng vai trò gì trong hệ thống DocLib?"
* **Prompt 2:** "Hôm nay thời tiết đẹp quá nhỉ."
* **Tiêu chí pass:** Luồng SSE trả về sự kiện `status` với data `Đang phản hồi trực tiếp`, không gọi tool nào cả.

### 2. Nhóm Kiểm Thử: Tác vụ API Nội bộ - Ví Điện Tử & Giao Dịch

Nhóm này ép Agentic Brain phải gọi `ActionAgent` để sử dụng các hàm tương tác với hệ thống tài chính.

* **Prompt 3 (Test `get_user_balance`):** "Kiểm tra số dư ví dl hiện tại của tôi là bao nhiêu."
* **Prompt 4 (Test `get_transaction_h
<truncated 3980 bytes>
sử dụng Docker." (Prompt này kiểm tra xem LangGraph có lập danh sách `steps` gồm nhiều bước gọi Tool nối tiếp nhau hay không).

### Hướng dẫn cách đánh giá kết quả (Góc độ Developer)

Khi chạy các Prompt trên gọi vào endpoint `/luong-du-lieu`, bạn cần theo dõi payload SSE (Server-Sent Events) trả về. Một tool được tính là **hoạt động tốt** khi:

1. Nhận được event `plan` trả về mảng Kế hoạch (steps) chính xác.
2. Nhận được event `status` thông báo đúng tên Tool đang chạy.
3. Nhận được event `tool` chứa kết quả trả về không dính các mã lỗi như `401`, `500` hoặc thông báo Fallback "Hệ thống đang gặp sự cố...".
4. Event `message` trả về câu trả lời tự nhiên dạng văn bản từ Aggregator Agent.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-05T19:11:45+07:00.

The user's current state is as follows:
Other open documents:
- /Users/caominhtrung/Library/Mobile Documents/com~apple~CloudDocs/Documents/DocLib/frontend/components/editor/LatexEditor.tsx (LANGUAGE_TSX)
- /Users/caominhtrung/Library/Mobile Documents/com~apple~CloudDocs/Documents/DocLib/frontend/components/editor/DocLibUndo.ts (LANGUAGE_TYPESCRIPT)
- /Users/caominhtrung/Library/Mobile Documents/com~apple~CloudDocs/Documents/DocLib/frontend/app/(main)/sang-tac/page.tsx (LANGUAGE_TSX)
- /Users/caominhtrung/Library/Mobile Documents/com~apple~CloudDocs/Documents/DocLib/frontend/app/(main)/tai-lieu/viewer/[id]/page.tsx (LANGUAGE_TSX)
- /Users/caominhtrung/Library/Mobile Documents/com~apple~CloudDocs/Documents/DocLib/frontend/components/editor/DocLibLatex.ts (LANGUAGE_TYPESCRIPT)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests/test_agentic_ai"]
pythonpath = ["agentic_ai/src"]
markers = [
    "unit: pure unit test (no external dependencies)",
    "integration: integration test (requires Docker services running)",
]
